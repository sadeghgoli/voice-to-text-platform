"""Reserved for a future streaming speech-to-text service.

WebSocket audio should call the same EngineRegistry used by the file worker.
This module is intentionally empty so the queue pipeline can stay unchanged.
"""
